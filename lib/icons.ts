import { MoonStar, Sun } from 'lucide-react-native';
import { iconWithClassName } from '~/utils';

export const IconSun = iconWithClassName(Sun);
export const IconMoonStar = iconWithClassName(MoonStar);
